%% in radians ... threshold at which pointa warning
pitch_roll_threshold (0.349).

% queried at runtime to determine which loader to use.  Choose
% one of the following:

% which_loader (morph_loader).
% which_loader (demonstrator_loader).
which_loader (jack_loader).
% which_loader (four_Loader).

% note: the above values must match the possible enumeration values
