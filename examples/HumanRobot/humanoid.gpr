with "../../pace.gpr";

project Humanoid is
   for Source_Dirs use (".");
   for Object_Dir use "obj";
   for Main use ("walk_main.adb");

   package Compiler is
      for Default_Switches ("Ada") use ("-g");
   end Compiler;
end Humanoid;
