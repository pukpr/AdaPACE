with "../../pace.gpr";

project Jpc is
   for Source_Dirs use (".");
   for Object_Dir use "obj";
   for Main use ("panda_control.adb");

   package Compiler is
      for Default_Switches ("Ada") use ("-g");
   end Compiler;
end Jpc;
