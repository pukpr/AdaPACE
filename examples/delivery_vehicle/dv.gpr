with "../../pace.gpr";

project DV is
   for Source_Dirs use (".");
   for Object_Dir use "obj";
   for Main use ("demo_drone.adb");

   package Compiler is
      for Default_Switches ("Ada") use ("-g");
   end Compiler;
end DV;
