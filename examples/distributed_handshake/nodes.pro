%%%% This is a 'nodes.pro' file.

connection (handshake, propose, 2).
connection (handshake, validate, 3).
connection (handshake, confirm, 1).

host_node (1, "localhost").
host_node (2, "localhost").
host_node (3, "localhost").
